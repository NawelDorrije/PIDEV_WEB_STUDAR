<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Facture Transport #{{ transport.id }}</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; margin: 20px; }
        .header { margin-bottom: 20px; overflow: hidden; }
        .logo { float: left; margin-right: 20px; }
        .invoice-details { float: right; text-align: right; }
        .details { margin: 20px 0; clear: both; }
        .company-details, .client-details { width: 48%; display: inline-block; vertical-align: top; }
        .company-details { float: left; }
        .client-details { float: right; }
        table { width: 100%; border-collapse: collapse; margin: 20px 0; clear: both; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        .total { font-weight: bold; font-size: 1.2em; }
        .footer { margin-top: 30px; text-align: center; font-size: 0.9em; }
    </style>
</head>
<body>
    <div class="header">
        <h1>Facture Transport #{{ transport.id }}</h1>
        <div class="invoice-details">
            <p><strong>Date:</strong> {{ invoiceData is defined ? invoiceData.date|date('d/m/Y') : "now"|date('d/m/Y') }}</p>
            <p><strong>Référence:</strong> {{ invoiceData is defined ? invoiceData.reference : 'INV-' ~ transport.id }}</p>
        </div>
        {% if logo_enabled is defined and logo_enabled %}
        <div class="logo">
           <img src="{{ base_path ~ asset('images/logo.png') }}" alt="Logo" width="150">
        </div>
        {% endif %}
    </div>
    
    <div class="details">
        <div class="client-details">
            <h2>Détails du client</h2>
            <p><strong>Nom:</strong> {{ transport.reservation.etudiant.nom }} {{ transport.reservation.etudiant.prenom }}</p>
            <p><strong>CIN:</strong> {{ transport.reservation.etudiant.cin }}</p>
            <p><strong>Email:</strong> {{ transport.reservation.etudiant.email }}</p>
        </div>
        
        {% if company_info is defined %}
        <div class="company-details">
            <h2>Informations de la société</h2>
            <p><strong>{{ company_info.name }}</strong></p>
            <p>{{ company_info.address }}</p>
            <p>{{ company_info.zip }} {{ company_info.city }}</p>
            <p>Tél: {{ company_info.phone }}</p>
            <p>Email: {{ company_info.email }}</p>
            <p>SIRET: {{ company_info.siret }}</p>
        </div>
        {% endif %}
    </div>
    
    <div class="transport-details">
        <h2>Détails du transport</h2>
        <table>
            <thead>
                <tr>
                    <th>Description</th>
                    <th>Montant (TND)</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Transport de {{ transport.reservation.adresseDepart }} à {{ transport.reservation.adresseDestination }}</td>
                    <td>{{ transport.tarif|number_format(2, ',', ' ') }}</td>
                </tr>
                {% if transport.extraCost is defined and transport.extraCost > 0 %}
                <tr>
                    <td>Frais supplémentaires</td>
                    <td>{{ transport.extraCost|number_format(2, ',', ' ') }}</td>
                </tr>
                {% endif %}
                <tr class="total">
                    <td><strong>Total</strong></td>
                    <td><strong>{{ invoiceData is defined ? invoiceData.amount|number_format(2, ',', ' ') : (transport.tarif + (transport.extraCost ?? 0))|number_format(2, ',', ' ') }}</strong></td>
                </tr>
            </tbody>
        </table>
    </div>
    
    <div class="footer">
        <p>Merci pour votre confiance!</p>
        <p>Cette facture a été générée automatiquement et ne nécessite pas de signature.</p>
        {% if transport.stripeInvoiceId is defined and transport.stripeInvoiceId %}
        <p>Status: Facturé via Stripe ({{ transport.stripeInvoiceId }})</p>
        {% else %}
        <p>Status: Facture générée</p>
        {% endif %}
    </div>
</body>
</html>