{% extends 'base.html.twig' %}

{% block title %}Mes Véhicules{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="mb-0">Mes Véhicules</h1>
            <a href="{{ path('app_voiture_new') }}" class="btn btn-primary">
                <i class="fas fa-plus"></i> Ajouter un véhicule
            </a>
        </div>

        {# Status Filter #}
        <div class="mb-4">
            <div class="btn-group" role="group">
                <a href="{{ path('app_voiture_index') }}" 
                   class="btn btn-sm"
                   style="{% if app.request.query.get('disponibilite') is null %}
                       background-color: transparent;
                       border-color: #0d6efd;
                       color: #0d6efd;
                       border: 1px solid;
                   {% else %}
                       color: #0d6efd;
                       border-color: #0d6efd;
                       background-color: transparent;
                       border: 1px solid;
                   {% endif %}">
                    Tous
                </a>
                <a href="{{ path('app_voiture_index', {'disponibilite': 'Disponible'}) }}" 
                   class="btn btn-sm"
                   style="{% if app.request.query.get('disponibilite') == 'Disponible' %}
                       background-color: #6FE69B;
                       border-color: #6FE69B;
                       color: #000;
                   {% else %}
                       color: #6FE69B;
                       border-color: #6FE69B;
                       background-color: transparent;
                       border: 1px solid;
                   {% endif %}">
                    Disponibles
                </a>
                <a href="{{ path('app_voiture_index', {'disponibilite': 'Non disponible'}) }}" 
                   class="btn btn-sm"
                   style="{% if app.request.query.get('disponibilite') == 'Non disponible' %}
                       background-color: #ffc107;
                       border-color: #ffc107;
                       color: #000;
                   {% else %}
                       color: #ffc107;
                       border-color: #ffc107;
                       background-color: transparent;
                       border: 1px solid;
                   {% endif %}">
                    Non disponibles
                </a>
            </div>
        </div>

        <div class="card shadow">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead class="table-light">
                            <tr>
                                <th>Modèle</th>
                                <th>Numéro de série</th>
                                <th>Image</th>
                                <th>Disponibilité</th>
                                <th>Date d'ajout</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for voiture in voitures %}
                            {% if voiture.utilisateur and voiture.utilisateur.cin == app.user.cin %}
                            <tr>
                                <td>{{ voiture.model }}</td>
                                <td>{{ voiture.numSerie }}</td>
                                <td>
                                    {% if voiture.image %}
                                        <img src="{{ vich_uploader_asset(voiture, 'imageFile') }}" 
                                             alt="{{ voiture.model }}" 
                                             class="img-thumbnail"
                                             style="max-height: 80px; max-width: 80px;">
                                    {% else %}
                                        <img src="{{ asset('images/default-car.jpg') }}"
                                             alt="{{ voiture.model }}" 
                                             class="img-thumbnail"
                                             style="max-height: 80px; max-width: 80px;">
                                    {% endif %}
                                </td>
                                <td>
                                    <span class="badge" 
                                          style="{% if voiture.disponibilite.value == 'Disponible' %}background-color: #6FE69B;
                                          {% elseif voiture.disponibilite.value == 'Non disponible' %}background-color: #ffc107;
                                          {% else %}background-color: #6c757d;{% endif %}">
                                        {{ voiture.disponibilite.value }}
                                    </span>
                                </td>
                                <td>{{ voiture.timestamp ? voiture.timestamp|date('d/m/Y H:i') : '' }}</td>
                                <td>
                                    <div class="d-flex gap-2">
                                        <a href="{{ path('app_voiture_show', {'idVoiture': voiture.idVoiture}) }}" 
                                           class="btn btn-sm btn-outline-info">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        <a href="{{ path('app_voiture_edit', {'idVoiture': voiture.idVoiture}) }}" 
                                           class="btn btn-sm btn-outline-primary">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <form method="post" action="{{ path('app_voiture_delete', {'idVoiture': voiture.idVoiture}) }}" 
                                              onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce véhicule ?');">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voiture.idVoiture) }}">
                                            <button type="submit" class="btn btn-link text-danger p-0 border-0" style="background: none;">
                                                <i class="fas fa-trash"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                            {% endif %}
                        {% else %}
                            <tr>
                                <td colspan="6" class="text-center py-4">
                                    <div class="text-muted">
                                        <i class="fas fa-car fa-3x mb-3"></i>
                                        <p>Aucun véhicule enregistré</p>
                                        <a href="{{ path('app_voiture_new') }}" class="btn btn-primary mt-2">
                                            Ajouter un véhicule
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        <a href="{{ path('app_transport_home') }}" class="btn btn-link text-decoration-none ps-0">
            <i class="fas fa-arrow-left"></i> Retour à l'accueil
        </a>
    </div>
{% endblock %}