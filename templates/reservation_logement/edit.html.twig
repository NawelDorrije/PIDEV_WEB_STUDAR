{% extends 'base.html.twig' %}

{% block title %}Edit ReservationLogement{% endblock %}

{% block stylesheets %}
<style>
  /* Edit Page Specific Styles */
  @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@600;700&display=swap');
  
  .reservationlogement-edit-section {
    padding: 80px 0;
    background-color: light;
  }

  .reservationlogement-edit-container {
    background: #F2F2F2;
    padding: 40px;
    border-radius: 15px;
    box-shadow: 0 10px 30px rgba(68, 83, 166, 0.15);
  }

  .reservationlogement-edit-title {
    font-family: 'Poppins', sans-serif;
    font-weight: 700;
    color: #4453A6;
    letter-spacing: 1.5px;
    text-align: center;
    margin-bottom: 40px;
    text-transform: uppercase;
  }

  .form-row {
    display: flex;
    flex-wrap: wrap;
    margin-bottom: 20px;
  }

  .form-group {
    flex: 0 0 50%;
    padding: 0 15px;
    margin-bottom: 20px;
  }

  .form-group label {
    display: block;
    font-weight: 600;
    color: #2c3e50;
    margin-bottom: 8px;
  }

  .reservationlogement-edit-input {
    width: 100%;
    height: 50px;
    border: 2px solid #dee2e6;
    border-radius: 8px;
    padding: 12px 15px;
    background: white;
    box-sizing: border-box;
  }

  .reservationlogement-edit-input:focus {
    border-color: #4453A6;
    box-shadow: 0 0 0 0.2rem rgba(68, 83, 166, 0.25);
    outline: none;
  }

  .reservationlogement-edit-action-buttons {
    display: flex;
    justify-content: center;
    gap: 20px;
    margin-top: 40px;
  }

  .reservationlogement-edit-btn-save {
    background-color: #4453A6;
    color: white;
    padding: 12px 30px;
    border-radius: 50px;
    font-weight: 600;
    border: none;
    transition: all 0.3s;
    box-shadow: 0 4px 15px rgba(68, 83, 166, 0.3);
    font-family: 'Poppins', sans-serif;
    letter-spacing: 1px;
  }

  .reservationlogement-edit-btn-cancel {
    background-color: #F2811D;
    color: white;
    padding: 12px 30px;
    border-radius: 50px;
    font-weight: 600;
    border: none;
    transition: all 0.3s;
    box-shadow: 0 4px 15px rgba(242, 129, 29, 0.3);
    font-family: 'Poppins', sans-serif;
    letter-spacing: 1px;
  }

  @media (max-width: 768px) {
    .form-group {
      flex: 0 0 100%;
    }
    
    .reservationlogement-edit-action-buttons {
      flex-direction: column;
      gap: 15px;
    }
    
    .reservationlogement-edit-btn-save, 
    .reservationlogement-edit-btn-cancel {
      width: 100%;
    }
  }
</style>
{% endblock %}

{% block body %}
<section class="reservationlogement-edit-section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="reservationlogement-edit-container">
                    <h2 class="reservationlogement-edit-title">Modifier Réservation</h2>
                    
                    {{ form_start(form, {'attr': {'class': 'rendezvous-edit-form'}}) }}
                        <div class="form-row">
                            <div class="form-group">
                                {{ form_label(form.dateDebut) }}
                                {{ form_widget(form.dateDebut, {'attr': {'class': 'reservationlogement-edit-input'}}) }}
                            </div>
                            <div class="form-group">
                                {{ form_label(form.dateFin) }}
                                {{ form_widget(form.dateFin, {'attr': {'class': 'reservationlogement-edit-input'}}) }}
                            </div>
                        </div>
                        
                        <div class="form-row">
                            <div class="form-group">
                                {{ form_label(form.proprietaire) }}
                                {{ form_widget(form.proprietaire, {'attr': {'class': 'reservationlogement-edit-input'}}) }}
                            </div>
                            <div class="form-group">
                                {{ form_label(form.etudiant) }}
                                {{ form_widget(form.etudiant, {'attr': {'class': 'reservationlogement-edit-input'}}) }}
                            </div>
                        </div>
                        
                        <div class="form-row">
                            <div class="form-group">
                                {{ form_label(form.idLogement) }}
                                {{ form_widget(form.idLogement, {'attr': {'class': 'reservationlogement-edit-input'}}) }}
                            </div>
                        </div>
                        
                        <div class="reservationlogement-edit-action-buttons">
                            <button type="submit" class="reservationlogement-edit-btn-save">
                                <i class="fas fa-check-circle mr-2"></i> Confirmer modifications
                            </button>
                            <a href="{{ path('app_reservation_logement_index') }}" class="reservationlogement-edit-btn-cancel">
                                <i class="fas fa-times-circle mr-2"></i> Annuler
                            </a>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}