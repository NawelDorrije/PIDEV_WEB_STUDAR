<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Dashboard Report</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            color: #333;
            margin: 0;
            padding: 20px;
        }
        h1 {
            text-align: center;
            color: #0059FF;
        }
        h2 {
            color: #333;
            border-bottom: 2px solid #0059FF;
            padding-bottom: 5px;
            margin-top: 20px;
        }
        .cards-container {
            display: flex;
            gap: 20px;
            margin-bottom: 20px;
        }
        .card {
            flex: 1;
            border: 1px solid #ddd;
            padding: 15px;
            text-align: center;
        }
        .card h3 {
            font-size: 18px;
            color: #333;
            margin-bottom: 10px;
        }
        .card p {
            margin: 5px 0;
            color: #606770;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        table, th, td {
            border: 1px solid #ddd;
        }
        th, td {
            padding: 10px;
            text-align: left;
        }
        th {
            background-color: #f4f4f4;
            color: #333;
        }
        .footer {
            text-align: center;
            font-size: 12px;
            color: #777;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <h1>Dashboard Report</h1>
    <p style="text-align: center;">Generated on {{ currentDate|date('Y-m-d H:i:s') }}</p>

    <h2>Summary</h2>
    <div class="cards-container">
        <div class="card">
            <h3>Le plus J'adore (❤️)</h3>
            <p>Logement : {% if mostJadore.logement %}{{ mostJadore.logement.adresse }}{% else %}Aucun{% endif %}</p>
            <p>Nombre : {{ mostJadore.count }}</p>
            <p>Propriétaire : {{ mostJadore.owner }}</p>
        </div>
        <div class="card">
            <h3>Le plus de Likes (👍)</h3>
            <p>Logement : {% if mostLikes.logement %}{{ mostLikes.logement.adresse }}{% else %}Aucun{% endif %}</p>
            <p>Nombre : {{ mostLikes.count }}</p>
            <p>Propriétaire : {{ mostLikes.owner }}</p>
        </div>
        <div class="card">
            <h3>Le plus de Dislikes (👎)</h3>
            <p>Logement : {% if mostDislikes.logement %}{{ mostDislikes.logement.adresse }}{% else %}Aucun{% endif %}</p>
            <p>Nombre : {{ mostDislikes.count }}</p>
            <p>Propriétaire : {{ mostDislikes.owner }}</p>
        </div>
    </div>

    <h2>Nombre d'Interactions (Top 7 Logements)</h2>
    <table>
        <thead>
            <tr>
                <th>Logement</th>
                <th>J'adore</th>
                <th>Likes</th>
                <th>Dislikes</th>
                <th>Shares</th>
            </tr>
        </thead>
        <tbody>
            {% if interactionData.labels is defined and interactionData.labels|length > 0 %}
                {% for i in 0..(interactionData.labels|length - 1) %}
                    <tr>
                        <td>{{ interactionData.labels[i] }}</td>
                        <td>{{ interactionData.jadoreCounts[i] }}</td>
                        <td>{{ interactionData.likesCounts[i] }}</td>
                        <td>{{ interactionData.dislikesCounts[i] }}</td>
                        <td>{{ interactionData.sharesCounts[i] }}</td>
                    </tr>
                {% endfor %}
            {% else %}
                <tr>
                    <td colspan="5">No data available</td>
                </tr>
            {% endif %}
        </tbody>
    </table>

    <h2>Most Active Owners</h2>
    <table>
        <thead>
            <tr>
                <th>Owner</th>
                <th>Post Count</th>
            </tr>
        </thead>
        <tbody>
            {% for owner in ownerPostCounts %}
                <tr>
                    <td>{{ owner.owner }}</td>
                    <td>{{ owner.postCount }}</td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="2">No data available</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="footer">
        <p>Generated by Your Application Name</p>
    </div>
</body>
</html>